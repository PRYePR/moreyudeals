{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "cd packages/web && yarn install && yarn build",
  "outputDirectory": "packages/web/.next",
  "installCommand": "cd packages/web && yarn install",
  "build": {
    "env": {
      "NODE_VERSION": "20"
    }
  }
}
